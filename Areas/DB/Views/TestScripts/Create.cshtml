@using BROWSit.Models;
@model BROWSit.Models.TestScriptCRUDModel

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()

    <fieldset>
        <legend>Requirement</legend>

        <h4>@Model.message</h4>

        <div class="editor-label">
            @Html.LabelFor(model => model.testScript.Title)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.testScript.Title)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.testScript.Author)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.testScript.Author)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.testScript.Path)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.testScript.Path)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.testScript.Requirements)
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.myRequirement, 
                new SelectList(Model.requirements, "ID", "Title"))
        </div>

        <p>
            <button class="btn btn-default" type="submit" name="submitType" value="create">Create</button>
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
